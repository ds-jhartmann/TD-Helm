apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-spring-app.fullname" . }}-test-connection"
  labels:
    {{- include "simple-spring-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-spring-app.fullname" . }}:{{ .Values.service.port }}/hello']
  restartPolicy: Never
